{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the assistant windows",
  "windows": ["main", "settings", "overlay", "bubble", "captions"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging",
    "dialog:allow-open",
    "dialog:allow-save",
    "opener:allow-open-url",
    "global-shortcut:default"
  ]
}
